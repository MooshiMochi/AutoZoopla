# Runtime dependencies. pyproject.toml is the source of truth; this file mirrors
# it for `pip install -r`. Dev/build extras: `pip install .[build,macos]`.
playwright>=1.40
pydantic>=2.0
PySide6>=6.7
pynacl>=1.5
keyring>=24
platformdirs>=4
